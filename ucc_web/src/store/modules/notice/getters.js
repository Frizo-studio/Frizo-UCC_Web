const followingNoticeCount = state => state.followingNoticeCount;

const eventNoticeCount = state => state.eventNoticeCount;

const chatNoticeCount = state => state.chatNoticeCount;

const getters = {
    followingNoticeCount,
    eventNoticeCount,
    chatNoticeCount
};

export default getters;